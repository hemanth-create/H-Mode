node_modules/
*.log
.DS_Store
.env.local
# transient benchmark scratch at the top level of results/:
benchmarks/results/*.json
# raw*/ (per-run model outputs) and *.md results are committed for audit
__pycache__/
*.pyc
.venv/

# local Claude Code editor state — not part of the package
.claude/
